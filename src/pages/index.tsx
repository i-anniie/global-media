import PublicLayout from "@/layout";
import TopArticles from "@/components/TopArticles";

const Home = () => {
  return (
    <PublicLayout title="Global Media | Home">
      <TopArticles />
    </PublicLayout>
  );
};

export default Home;
